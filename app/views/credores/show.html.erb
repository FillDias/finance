<% content_for :title, @credor.nome %>
<div class="page-header">
  <h1><%= @credor.nome %></h1>
</div>

<section>
  <h2>Cartões</h2>
  <% if @credor.cartoes.empty? %>
    <p class="vazio">Nenhum cartão cadastrado para este credor.</p>
  <% else %>
    <table class="tabela-densa">
      <thead>
        <tr>
          <th>Nome</th>
          <th>Limite</th>
          <th>Fechamento</th>
          <th>Vencimento</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @credor.cartoes.order(:nome).each do |cartao| %>
          <tr>
            <td><%= cartao.nome %></td>
            <td class="valor"><%= moeda(cartao.limite_total) %></td>
            <td>dia <%= cartao.dia_fechamento %></td>
            <td>dia <%= cartao.dia_vencimento %></td>
            <td class="acoes"><%= link_to "Editar", edit_cartao_path(cartao) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>
</section>

<section>
  <h2>Empréstimos</h2>
  <p class="vazio">Módulo de Empréstimos ainda não implementado.</p>
</section>

<%= link_to "Voltar", credores_path, class: "link-voltar" %>
