<% content_for :title, "Escolher perfil" %>
<div class="page-header">
  <h1>Quem está usando?</h1>
</div>

<section class="grid-perfis">
  <% @perfis.each do |perfil| %>
    <%= button_to perfil.nome, selecionar_perfil_path(perfil), method: :post, class: "perfil-card" %>
  <% end %>
</section>
